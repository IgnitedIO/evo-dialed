
/*require('dotenv').config(); // if you're using a .env file
const {
  scrapeCreators_getInstagramProfile,
  scrapeCreators_getTiktokProfile,
  scrapeCreators_getInstagramAnalytics,
  scrapeCreators_getTiktokAnalytics
} = require('./external_apis/scrapecreators'); // update path as needed

(async () => {
  try {
    // Test Instagram profile
    //const igProfile = await scrapeCreators_getInstagramProfile('nasa');
    //console.log('Instagram Profile:', igProfile);

    // Test TikTok profile
    //const tiktokProfile = await scrapeCreators_getTiktokProfile('charlidamelio');
    //console.log('TikTok Profile:', tiktokProfile);

    // Test Instagram Reels
   // const igAnalytics = await scrapeCreators_getInstagramAnalytics(['complex']);
   // console.log('Instagram Analytics:', igAnalytics);

    // Test TikTok Videos
    //const tiktokAnalytics = await scrapeCreators_getTiktokAnalytics(['charlidamelio']);
    //console.log('TikTok Analytics:', tiktokAnalytics);

  } catch (err) {
    console.error('Test Error:', err);
  }
})();
*/
