// Component Imports
import { InternalMetricsHome } from "../../../Metrics/Home";

// Metrics Tab Component
export const MetricsTab = ({ creatorId }) => {
	return <InternalMetricsHome creatorId={creatorId} />;
}